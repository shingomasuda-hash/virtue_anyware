# ASSUMPTIONS — 仕様が未確定な箇所で置いた仮定

要件に明示がない点は作業を止めず、合理的な仮定を置いて実装した。
実データ・実運用の確認が取れ次第、ここを起点に修正する。

---

## A. 技術選定

| # | 仮定 | 根拠 / 影響 |
| --- | --- | --- |
| A-1 | 認証は **Better Auth 1.7（安定版）** を使う | 要件は「安全性の高い認証ライブラリの**現在の安定版**」を指定。2026-09 時点で Auth.js v5 (`next-auth`) は `latest` が `5.0.0-beta.32` で安定版が無い。Auth.js の安定版採用が必須なら差し替え可能（`src/server/auth/` に隔離済み） |
| A-2 | Prisma は 7.x（`@prisma/client` の `latest`）。`prisma@8` は RC のため採用しない | Prisma 7 はドライバアダプタ必須。`@prisma/adapter-pg` を使用し、Neon へは `@prisma/adapter-neon` へ 1 行差し替えで移行できる |
| A-3 | shadcn/ui は CLI ではなくリポジトリ内実装 | ネットワーク依存なくビルドできる。`src/components/ui` が実体で、shadcn のパターン（Radix + CVA + tailwind-merge）に従う |
| A-4 | DB の**テーブル名は snake_case**（`@@map`）、**カラム名は Prisma 既定の camelCase** | ドキュメントは論理名を snake_case で記述している。BI ツール等から直接 SQL を書く要件が出た場合は `@map` でカラム名も snake_case 化する |
| A-5 | 領収書・請求書 PDF は URL（`receiptUrl` / `pdfUrl`）で保持し、ファイル実体は外部ストレージに置く | Vercel 前提のためローカル FS に置かない。S3 / Vercel Blob / Supabase Storage のいずれでも接続できる |

## B. データモデル

| # | 仮定 | 根拠 / 影響 |
| --- | --- | --- |
| B-1 | §44 の `event_expenses` は独立テーブルにせず、**`expenses.event_id` が入った行**として表現 | 「必要に応じて正規化」の指示に従った。催事費・本部経費・代理店経費を同じ承認フロー・同じ会計 CSV で扱える |
| B-2 | §80 の `vendors` は `partners` に統合し、`kinds[]`（`TOSSUP` / `VENDOR` / `SUPPLIER` / `OTHER`）で区別 | トスアップ先・経費先・上位会社が同一法人になりうる。請求書/支払の取引先参照を多態にしないで済む |
| B-3 | §43 のブース位置詳細は `booths` のカラムとして持つ（別テーブルにしない） | ブース 1 件に対し 1 組の属性しか存在しないため。写真は `photos`(Json) に URL 配列で保持 |
| B-4 | `staff` はアプリのログインユーザーと別概念。`staff.user_id` は任意リンク | 催事のアルバイトはシステムアカウントを持たないことが多い |
| B-5 | 催事に複数代理店が参加しうるため `event_agencies` 中間表を用意。`events.agency_id` は主担当代理店 | - |
| B-6 | `contracts.quantity` を汎用数量、`contract_watt` を電力向けの別カラムとして併存させる | §6 が `contract_watt` を明示。将来商材（ガス・通信）は `quantity` + `products.quantity_unit` で扱う |
| B-7 | 顧客・契約・代理店・経費は論理削除（`deleted_at`）。物理削除しない | 監査・精算の追跡性を確保するため |
| B-8 | `event_metrics` の複合ユニークは `hour_slot` / `booth_id` が NULL のとき Postgres 上で重複を防げない | 日次集計行は アプリ側（service）で存在チェックしてから作成する。将来 `UNIQUE NULLS NOT DISTINCT` の部分索引へ移行予定 |

## C. 収益・精算

| # | 仮定 | 根拠 / 影響 |
| --- | --- | --- |
| C-1 | 単価の適用基準日は **契約日（`contracted_at`）**。無ければ申込日、それも無ければ処理日 | 「適用期間を持たせる」要件から、契約成立時点の単価で確定させるのが自然 |
| C-2 | 代理店支払単価は `agency_unit_prices`（代理店マスタ側）を優先し、無ければ `pricing_rules(AGENCY_PAYOUT)` にフォールバック | §5 と §6 の双方を満たすため |
| C-3 | 単価ルールが複数該当する場合、**specificity（agency/product/supplier/plan の一致数）→ priority → effective_from 降順** で 1 件を選ぶ | 「代理店ごとに異なる単価」と「全社共通単価」を共存させるため |
| C-4 | 集計対象は `contract_statuses.is_cancelled = false` の契約。不備・保留は**集計に含める**（売上見込みとして扱う） | §35 は「キャンセル契約の除外」のみを指定。不備の扱いは運用確認待ち |
| C-5 | 金額は円単位の整数へ四捨五入（`Math.round`）。単価は小数第4位まで保持 | 0.5円/W のような単価に対応するため |
| C-6 | 精算の最終支払額 = `payout − adjustment − cancel_deduction` | §25 の項目順から推定 |
| C-7 | 消費税は原則 10%・税込入力を既定とし、`tax_category` で軽減税率・非課税を選べる | 会計 CSV 出力を見据えた最小構成 |
| C-8 | 代理店の支払サイクル既定は「月末締め・翌月末払い」（`payment_closing_day` 等で代理店ごとに変更可） | - |
| **C-9** | **季節係数は「乗算」で適用する（想定使用量 = 明細の使用量 × 係数）** | 条件表に乗除の明記が無い。使用量のピーク月（1月 86.9% / 8月 86.7%）の係数が 100% 未満であることから「実績を平準化する乗算」と解釈した。**要確認事項**。除算が正しい場合は `usage.ts` の `SEASONAL_COEFFICIENT_MODE` の切替のみで対応できる |
| C-10 | 「代理店fee + 10%」の 10% は**業務管理費を控除する前**の手数料に掛かる | 控除は VIRTUE と代理店の間の精算であり、上位会社からの受取額には影響しないという読み。結果として控除分は VIRTUE の粗利になる |
| C-11 | 代理店支払単価は `agency_unit_prices` と `pricing_rules` の**より具体的な方**を採用する | 「代理店マスタを常に優先」にすると汎用の 円/W 単価が供給元固有の階段表を握りつぶし、手数料が 0 円になる（実機検証で検出） |
| C-12 | CSV に検針月が無い場合は契約日の月を使い、警告を出す | 検針月によって手数料が大きく変わるため、黙って補完せず必ず警告する |
| C-13 | 階段表方式では契約ワット数を必須にしない（使用量があればよい） | 明細なしの定額手数料だけで成立する案件もあるため、数量の必須判定は明細提出の有無で切り替える |
| C-14 | 戻入の「1 か月目」は基準日の属する月そのものを指す | 条件表の「◯月目の末日まで」という表記に合わせた。申込 2026-09-15 → 3 か月目末日 = 2026-11-30 |
| C-15 | 戻入リスクの評価は読み取り時に都度計算し、`clawbacks` テーブルは確定分の記録に使う | 期限だけで機械的に判定できるため、日次バッチを前提にしない設計にした |

## D. CSV 取込

| # | 仮定 | 根拠 / 影響 |
| --- | --- | --- |
| D-1 | サンプル CSV が未提供のため、§8 の項目を `src/server/services/csv/field-catalog.ts` に定義。実ファイル受領後は **alias を追加するだけ**で対応する | 列名の表記ゆれはカタログの `aliases` で吸収する |
| D-2 | 文字コードは「UTF-8 BOM → UTF-8 → CP932」の順で自動判定。ユーザーが STEP2 で手動上書きできる | §2 の Shift-JIS 対応要件 |
| D-3 | ワット数は `5,000` / `5000W` / `5kW` を受け付け、**kW 表記は 1000 倍して W に正規化**する | 実運用で混在しうるため |
| D-4 | 電話番号＋氏名の一致は「同一顧客」として自動的に既存顧客へ紐づける（契約は新規追加） | §9 の優先順位 3 番目。設定で `NEEDS_REVIEW` に変更できるようテンプレート側に `dedupe_strategy` を用意 |
| D-5 | 電話番号のみ一致 / 氏名+生年月日 / 氏名+郵便番号 は**自動登録しない**（確認画面へ） | §9 の「完全一致でない場合は自動登録しない」 |
| D-6 | ロールバックは「そのバッチが新規作成したレコードのみ削除」＋「更新行は `before_snapshot` から復元」 | 他バッチの更新を巻き戻さないため |
| D-7 | CSV の最大サイズは 20MB、1 ファイル 5 万行までを想定 | 超える場合は分割アップロードを案内する |
| D-8 | STEP1〜5 の中間状態は `import_batches`(DRAFT) + `import_rows`(PENDING) に保持する | ページを離れても続きから再開できる。**業務データ（顧客・契約）には一切書き込まない**ため「DB へ書き込まない DRY RUN」の要件は満たしている |
| D-9 | プレビュー・DRY RUN・確定はすべて同じ `planImport()` を使い、確定時にもう一度評価する | プレビュー後にデータが変わっていても矛盾しない。「プレビューでは成功したのに確定で失敗」を構造的に防ぐ |
| D-10 | CSV に単価列があっても、金額は**必ず単価マスタから再計算**する | CSV の単価は参考値として突合し、不一致なら警告のみ出す。金額の唯一の正はマスタ（§6） |
| D-11 | 同一ファイル内に同じ契約番号が複数行ある場合、最初の行のみ取り込む | 2 行目以降は「重複」として警告つきでスキップ |
| D-12 | 代理店が解決できない行の扱いは取込オプションで エラー / 警告 を選べる（既定はエラー） | 代理店マスタ整備前の暫定取込にも対応できるようにした |
| D-13 | 未知の契約ステータスはエラー。既定ステータスを設定すればそれで補完する | ステータス表記ゆれをマスタ側で吸収させる方針 |
| D-14 | ロールバックは「取込直後の `updatedAt`」と現在値の比較で人手変更を検出する | タイムスタンプ比較のため、同一ミリ秒内の変更は検出できない。実運用では問題にならないと判断した |
| D-15 | ロールバックは論理削除（`deletedAt`）で行い、物理削除しない | 監査要件。売上・精算が紐づいた契約は巻き戻さない |
| D-16 | アップロードは Server Action ではなく Route Handler（`/api/import/upload`） | Server Action のボディサイズ上限に縛られず 20MB を受け取るため。認証・認可・レート制限はハンドラ内で実施 |

## E. アップセルCRM

| # | 仮定 | 根拠 / 影響 |
| --- | --- | --- |
| E-1 | アップセルは **VIRTUE 本部の商流**。代理店ロールには既定で非公開 | §3 の代理店権限一覧にアップセルが含まれないため。公開が必要なら `permissions.ts` の 1 行で切り替えられる |
| E-2 | 電力契約（キャンセル以外）の登録時にアップセルリードを 1 件自動生成する | §18「電力顧客登録後に自動的にアップセル対象リストへ表示」 |
| E-3 | 1 顧客 × 1 商材につきリードは 1 件（`@@unique([customerId, productId])`） | 同じ顧客に太陽光と蓄電池の 2 リードは持てる |
| E-4 | 太陽光の紹介利益は `revenues.referral_fee`（原価 0）として計上し、粗利＝紹介料とする | §64 の LTV 例（紹介利益をそのまま利益として加算）に合わせた |
| E-5 | ファネルの「接続」は `upsell_statuses.funnel_stage` が CONNECTED 以降のリード数で数える | 架電ログ単位ではなくリード単位。定義は `docs/KPI_DEFINITIONS.md` に統一 |

## F. 経営管理・分析

| # | 仮定 | 根拠 / 影響 |
| --- | --- | --- |
| F-1 | 「営業利益相当額」は **承認済み経費のみ**を控除して算出する | 申請中の経費で数字がぶれないようにするため |
| F-2 | 催事経費 = `expenses(event_id)` + ブース固定費 + シフト人件費。ブース固定費を `expenses` にも二重登録しない運用を前提 | 二重計上を避けるため、ブース代は `booths` の固定費カラムを正とする（デモデータでは検証のため両方に入れている） |
| F-3 | 時間帯スロットは 2 時間刻み（10-12 / 12-14 / 14-16 / 16-18 / 18-20 / 20-）を既定とする | §59 の例に合わせた。`event_metrics.hour_slot` は開始時刻(整数)で保持 |
| F-4 | 祝日判定は `event_metrics.is_holiday` の手入力を正とする（祝日マスタは持たない） | 日本の祝日は法改正で動くため、外部ライブラリ導入は PHASE 9 で検討 |
| F-5 | 原価配賦の按分結果は確定保存し、按分元（契約件数・売上）が後から変わっても再計算しない | 過去月の損益を動かさないため |
| F-6 | ROI は一貫して `利益 ÷ 投下コスト`。「ブース代ROI」は分母がブース関連費、「催事ROI」は分母が催事総経費 | 同一 KPI が画面ごとに違う式にならないよう `docs/KPI_DEFINITIONS.md` に固定 |
| F-7 | 率系 KPI の分母が 0 のときは 0 を返す。ただし「前月比」など比較不能を示すべきものは `null`（UI では `—`） | `safeDivide` / `safeRatioOrNull` に一元化 |
| F-8 | 催事LTV は「その催事で獲得した顧客（`customers.source_event_id`）が生んだ全売上」で算出する | §65/§66 |

## G. 運用・セキュリティ

| # | 仮定 | 根拠 / 影響 |
| --- | --- | --- |
| G-1 | テナントは当面 VIRTUE 1 組織。ただし全 fact に `organization_id` を持たせ将来の複数社化に備える | - |
| G-2 | ユーザーの新規登録は管理者による招待/作成のみ（セルフサインアップは無効） | 個人情報を扱うため |
| G-3 | セッション有効期限 7 日 / 更新 1 日。ログイン試行は IP+メールで 5 回/分 | §31 のレート制御 |
| G-4 | PII（電話・住所・生年月日）は一覧で部分マスクし、詳細画面でのみ全表示 | §31。CSV エクスポートは `export:csv` 権限と監査ログを必須にする |
| G-5 | デモ用パスワード `Virtue#2026` は開発専用。本番投入前に必ず全アカウントを再作成する | README にも明記 |
| G-6 | 監査ログは全 UPDATE ではなく「§27 に列挙された重要操作」のみ before/after を記録する | ログ量の爆発を避けるため |
| G-7 | デモパスワードは開発・テスト環境でのみ既定値を使い、それ以外ではランダム生成する | `SEED_DEMO_PASSWORD` で明示指定も可能（`prisma/seed/guard.ts`） |
| G-8 | `NODE_ENV=production` または DATABASE_URL に `prod`/`production` を含む場合、シードは既定で中止する | 本番 DB へ既知パスワードのアカウントを流し込まないため。`ALLOW_DEMO_SEED=true` で明示的に上書きできる |
| G-9 | 本番投入前チェック `npm run preflight` を必須とする | 開発既定の秘密鍵・デモアカウント・既知パスワードの残存を機械的に検出し、1 件でもあれば非ゼロ終了する |
| G-10 | 本番投入時は**全アカウントを再作成**する前提 | デモアカウントは preflight でエラーになるため、そのままでは本番に持ち込めない |
| G-11 | CSV アップロードは IP（ユーザー）単位で 10 回/分に制限する | 大きなファイルの連投による DoS を避けるため。インメモリ実装のため、複数インスタンス構成では Redis 等への移行が必要 |
| G-12 | Server Action は必ず `runAction()`（認証 → 認可 → Zod → 例外正規化）を経由する | 「認可を書き忘れた Server Action」が発生しない構造にするため |

## D2. 案件管理（太陽光・蓄電池）

提供された「顧客・案件管理シート」（ダミーデータ 顧客40件 / 案件40件 / 進捗21件 / 報酬21件）から
読み取れた内容を根拠にしている。列名・略語の意味が違う場合はここを起点に直す。

| # | 仮定 | 根拠 / 影響 |
| --- | --- | --- |
| D2-1 | 案件ステータスの略語を `FO` =「商談化せず失注」、`CLO` =「提案条件が合わず失注」、`B` =「ローン審査否決」と解釈した | シートの失注理由列がそれぞれ「条件不一致のため商談化せず」「提案条件が合わず見送り」「ローン審査否決」で固定されていた。呼称が違う場合は `deal_statuses.label` の変更だけで済む |
| D2-2 | コミッションの端数は**円未満を四捨五入**して保存する | 提供シートには浮動小数点誤差が残っており（A0027 の営業コミッションが `489999.99999999994`。正しくは 1,400,000 × 0.35 = 490,000）、支払額に 1 円未満の端数が入っていた。実装では `roundYen()` を通して必ず整数円にする。切り捨て運用に変えたい場合は `roundYen()` の呼び出し 1 箇所を差し替える |
| D2-3 | 「自社」は代理店マスタに登録せず `deals.agencyId = NULL`（本部直販）として扱う | 既存の顧客・契約と同じ扱いに揃えるため。代理店ロールからは自社直販案件が一切見えなくなる |
| D2-4 | 控除額・コミッション率は**案件ごとに入力**する（代理店マスタの既定値から自動設定しない） | シートが案件単位で率を持ち、同じ代理店でも案件により率が異なっていた（代理店A で 0.2 と 0.3 が併存） |
| D2-5 | 報酬の計算結果（原価合計・営業利益・各コミッション・会社残粗利）を列として保存する | 率や控除額の運用変更で過去の支払額が動かないようにするため。電力の単価スナップショット（§6）と同じ方針 |
| D2-6 | 1 顧客に複数案件を持てる構造にする（シートのダミーは 1:1 だった） | 蓄電池の追加販売など再商談を想定。`deals.code` を業務キーとして一意にする |
| D2-7 | 進捗の各項目は列ごとに取りうる値が違うため、共通の 4 値（未対応/対応中/完了/不要）ではなく項目別 enum にした | 実データが「ローン審査: 仮審査済/本審査中/本審査済」「現調: 日程調整中/予定」など列固有の値を持っており、共通化すると情報が落ちる |
| D2-8 | 顧客の DriveフォルダURL は `customers.driveFolderUrl` として保持する（ファイル実体は持たない） | シートの運用方針（身分証・ローン申込書は権限制限した Drive に置く）をそのまま踏襲する。A-5 と同じ考え方 |
| D2-9 | 補助金は「制度名・申請日・交付決定日」のみを持ち、金額は持たない | シートに金額列が無かった。交付額を収益に計上する要件が出たら `Revenue` 側へ追加する |
| D2-10 | 報酬（原価・営業利益・会社残粗利）は代理店ロールに一切返さない | §17 の「本部の粗利・粗利率は代理店に表示禁止」を厳格に適用した。代理店に自社コミッションのみ開示する要件が出た場合は `maskDealFinancials()` で対応できる |
| D2-11 | コミッション率は 0〜1（30% なら 0.3）で入力する。1 を超える値は桁誤りとして拒否する | `30` と入力された場合に控除後粗利の 30 倍を支払う計算になってしまうため。営業率+代理店率の合計が 1 を超えることは許容する（意図的な赤字配分を止めない） |
